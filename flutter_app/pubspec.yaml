name: flutter_app
description: "CineCritique - Die Filmdatenbank"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.2.0
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  openid_client: ^0.4.9
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true